spring:
  application:
    name: EMPLOLYEE-SEARCH-DASHBOARD
server:
  port: 8081
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9091/eureka
  instance:    
      hostname: localhost 
